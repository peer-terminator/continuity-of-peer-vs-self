# Copy to `.env` (gitignored) or export these in your shell. Only these three
# names are read (providers.load_dotenv); a variable already set in the
# environment is left alone; values are never printed or logged.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=
